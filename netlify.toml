[build]
  command = "npm run build"
  publish = "dist/oaw/browser"

[build.environment]
  NODE_VERSION = "20"

# Angular SSR plugin configuration
# This plugin automatically handles Angular SSR on Netlify
[[plugins]]
  package = "@netlify/angular-runtime"
